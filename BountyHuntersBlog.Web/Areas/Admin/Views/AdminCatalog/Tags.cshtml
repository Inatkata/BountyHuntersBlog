@using Microsoft.EntityFrameworkCore
@model IReadOnlyList<BountyHuntersBlog.Services.DTOs.TagDto>
@{
    ViewData["Title"] = "Tags";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<section class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">Tags</h2>
        <a asp-action="TagEdit" class="btn btn-primary">Create</a>
    </div>

    @if (!Model.Any())
    {
        <div class="alert alert-info">No tags.</div>
    }
    else
    {
        <table class="table table-striped align-middle">
            <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th class="text-end">Actions</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var t in Model)
            {
                <tr>
                    <td>@t.Id</td>
                    <td>@t.Name</td>
                    <td class="text-end">
                        <a asp-action="TagEdit" asp-route-id="@t.Id" class="btn btn-sm btn-outline-secondary">Edit</a>
                        <form asp-action="TagDelete" method="post" class="d-inline">
                            <input type="hidden" name="id" value="@t.Id" />
                            <button class="btn btn-sm btn-outline-danger" type="submit"
                                    onclick="return confirm('Delete tag?')">Delete</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
</section>